sudo: required
addons:
  apt:
    packages:
      - python-setuptools
      - cmake
      - ninja-build
      - libssl-dev
      - srecord
script:
  - chmod +x ./Scripts/Build.sh
  - ./Scripts/Build.sh
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: Build
  on:
    branch: master
