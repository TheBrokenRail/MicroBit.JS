sudo: required
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - python-setuptools
      - cmake
      - build-essential
      - ninja-build
      - libssl-dev
      - autoconf2.13
      - gcc
script:
  - chmod +x ./Scripts/Install.sh
  - ./Scripts/Install.sh
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: Build
  on:
    branch: master
